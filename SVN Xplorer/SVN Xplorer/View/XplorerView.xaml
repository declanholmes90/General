<UserControl x:Class="SVN_Xplorer.View.XplorerView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SVN_Xplorer.View"
      xmlns:viewModel="clr-namespace:SVN_Xplorer.ViewModel"
      xmlns:model="clr-namespace:SVN_Xplorer.Model"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      d:DataContext="{Binding viewModel:XplorerViewModel}"
      >
    <Grid Background="AliceBlue">
        <TreeView ItemsSource="{Binding TreeViewRoots}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Nodes}">
                    <TextBlock Text="{Binding Path=Text}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
